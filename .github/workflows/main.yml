name: Shell Command

on: [Push]

jobs:
run-shell-command:
    runs-on: ubuntu-latest
    steps:
        -name: echo a string
         run: echo "Hello World"
        -name: multiplescript
         run: |
          node -v
          npm -v

          
